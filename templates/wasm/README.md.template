# {pkg_name}

Self-contained Lumis language package for {languages_text}.

Built with [`tree-sitter build --wasm`](https://tree-sitter.github.io/tree-sitter/cli/build.html) `v{tree_sitter_cli_version}`

Used by [Lumis](https://lumis.sh) and compatible with [web-tree-sitter](https://www.npmjs.com/package/web-tree-sitter) v{tree_sitter_cli} and other WASM runtimes.

The default export is a `URL` pointing at `{wasm_name}.wasm`, so the parser is
stored only once and can be handled as an asset by Node.js and browser bundlers.
`language.json` keeps the parser version, integrity, grammar name, aliases, and
matching highlight, injection, locals, and bracket queries together.

## Parser

| Parser | Version | Rev |
| ------ | ------- | --- |
| [{git_url}]({git_url}) | {upstream_version} | {rev} |

## CDN

- [https://unpkg.com/{pkg_name}@{npm_version}/{wasm_name}.wasm](https://unpkg.com/{pkg_name}@{npm_version}/{wasm_name}.wasm)
- [https://cdn.jsdelivr.net/npm/{pkg_name}@{npm_version}/{wasm_name}.wasm](https://cdn.jsdelivr.net/npm/{pkg_name}@{npm_version}/{wasm_name}.wasm)

## License

This package redistributes the parser source listed above; see that project for its license.
