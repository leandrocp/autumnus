; Complete SQL read, write, or schema structures.
(statement
  (select
    (select_expression))
  (from)) @guess

(create_table) @guess

(insert) @guess

(cte
  (statement
    (select)
    (from))) @guess
