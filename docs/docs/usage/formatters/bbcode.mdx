---
sidebar_position: 5
slug: /formatters/bbcode
title: BBCode Formatter
description: Options and examples for Lumis BBCode output with scope-based tags.
keywords:
  - lumis bbcode
  - bbcode formatter
  - forum syntax highlighting
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# BBCode Formatter

Scope-based BBCode tags for forum posts and BBCode-based platforms.

## Options

| Runtime | Options |
| --- | --- |
| Rust | `lang` |
| CLI | (none) |

## Example

<Tabs groupId="runtime" queryString="lang" defaultValue="rust">
  <TabItem value="rust" label="Rust">

```rust
use lumis::{highlight, BBCodeBuilder, languages::Language};

let bbcode = highlight(
    "fn main() {}",
    BBCodeBuilder::new()
        .lang(Language::Rust)
        .build()
        .unwrap(),
);

println!("{}", bbcode);
```

  </TabItem>
  <TabItem value="cli" label="CLI">

```bash
lumis highlight main.rs -f bbcode
```

  </TabItem>
</Tabs>

## Output format

Scope names from the syntax tree are converted to BBCode tags by replacing dots with hyphens:

```
[keyword-function]fn[/keyword-function] [function]main[/function]
```

## When to use it

- Forum posts and BBCode-based platforms
- Custom rendering pipelines that consume scope-tagged text
- Lightweight markup without HTML overhead
