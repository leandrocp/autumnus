---
sidebar_position: 4
slug: /formatters/terminal
title: Terminal Formatter
description: Options and examples for Lumis ANSI terminal output.
keywords:
  - lumis terminal
  - ansi
  - terminal formatter
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Terminal Formatter

ANSI escape codes for shell output.

## Options

| Runtime | Options |
| --- | --- |
| Rust | `lang`, `theme` |
| Elixir | `:theme` |
| JavaScript | `language`, `theme` |
| CLI | `--theme` |

## Example

<Tabs groupId="runtime" queryString="lang" defaultValue="javascript">
  <TabItem value="javascript" label="JavaScript">

```js
import {highlight} from '@lumis-sh/lumis'
import {terminal} from '@lumis-sh/lumis/formatters'
import javascript from '@lumis-sh/lumis/langs/javascript'
import dracula from '@lumis-sh/themes/dracula'

const ansi = await highlight('const x = 1', terminal({language: javascript, theme: dracula}))
console.log(ansi)
```

  </TabItem>
  <TabItem value="rust" label="Rust">

```rust
use lumis::{highlight, TerminalBuilder, languages::Language, themes};

let ansi = highlight(
    "fn main() {}",
    TerminalBuilder::new()
        .lang(Language::Rust)
        .theme(Some(themes::get("dracula").unwrap()))
        .build()
        .unwrap(),
);

println!("{}", ansi);
```

  </TabItem>
  <TabItem value="elixir" label="Elixir">

```elixir
Lumis.highlight!(
  "fn main() {}",
  language: "rust",
  formatter: {:terminal, theme: "dracula"}
)
```

  </TabItem>
  <TabItem value="cli" label="CLI">

```bash
lumis highlight main.rs --theme dracula
```

  </TabItem>
</Tabs>

## When to use it

- CLI tools
- logs and local scripts
- previewing highlighted code in a terminal
