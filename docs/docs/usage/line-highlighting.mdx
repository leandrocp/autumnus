---
sidebar_position: 4
slug: /recipes/line-highlighting
title: Line Highlighting
description: Highlight selected lines in Lumis for inline HTML, linked HTML, and CLI workflows.
keywords:
  - lumis line highlighting
  - highlighted lines
  - html inline
  - html linked
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Line Highlighting

Line highlighting lets you emphasize key lines without changing the rest of the block.

## Common patterns

- highlight a few exact lines
- highlight a range such as `3..5`
- use theme styling or your own CSS class / inline style

## Examples

<Tabs groupId="runtime" queryString="lang" defaultValue="javascript">
  <TabItem value="javascript" label="JavaScript">

```jsimport {highlight} from '@lumis-sh/lumis';
import {htmlInline} from '@lumis-sh/lumis/formatters';
import javascript from '@lumis-sh/lumis/langs/javascript';
import dracula from '@lumis-sh/themes/dracula';

const html = await highlight(
  'line 1\nline 2\nline 3\nline 4',
  htmlInline({
    language: javascript,
    theme: dracula,
    highlightLines: {lines: [1, [3, 4]], style: 'theme'},
  })
);
```

For linked HTML, use `htmlLinked({ highlightLines: { lines: [1, [3, 4]], class: 'active-line' } })`.

  </TabItem>
  <TabItem value="rust" label="Rust">

```rustuse lumis::{highlight, HtmlInlineBuilder, languages::Language, themes};
use lumis::formatter::html_inline::{HighlightLines, HighlightLinesStyle};

let highlight_lines = HighlightLines {
    lines: vec![1..=1, 3..=4],
    style: Some(HighlightLinesStyle::Theme),
    class: None,
};

let formatter = HtmlInlineBuilder::new()
    .lang(Language::PlainText)
    .theme(Some(themes::get("dracula").unwrap()))
    .highlight_lines(Some(highlight_lines))
    .build()
    .unwrap();

let html = highlight("line 1\nline 2\nline 3\nline 4", formatter);
```

  </TabItem>
  <TabItem value="elixir" label="Elixir">

```elixirLumis.highlight!(
  "line 1\nline 2\nline 3\nline 4",
  language: "plaintext",
  formatter: {:html_inline,
    theme: "dracula",
    highlight_lines: %{
      lines: [1, 3..4],
      style: :theme
    }
  }
)
```

For linked HTML, switch to `:html_linked` and set `class: "active-line"`.

  </TabItem>
  <TabItem value="cli" label="CLI">

```bashlumis highlight main.rs --formatter html-inline --highlight-lines 1,3-5,10
```

  </TabItem>
</Tabs>

## What changes between formatters

| Formatter | Highlight style |
| --- | --- |
| inline HTML | theme style or custom inline CSS |
| linked HTML | CSS class names |
| multi-theme HTML | theme style or custom inline CSS |
| terminal | no line highlight styling |

