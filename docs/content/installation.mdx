---
sidebar_position: 2
slug: /installation
title: Installation
description: Install Lumis for CLI, Rust, Elixir, JavaScript, Browsers / CDN, and Java.
keywords:
  - lumis installation
  - cargo
  - npm
  - hex
  - maven
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Installation

Pick the runtime you want to use first. The highlighting concepts stay the same across all of them.

<Tabs groupId="runtime" queryString="lang" defaultValue="javascript">
  <TabItem value="javascript" label="JavaScript">

```bash
npm install @lumis-sh/lumis @lumis-sh/themes
```

Use `@lumis-sh/lumis` for the per-language WASM runtime and
`@lumis-sh/themes` for bundled themes. Node.js, Bun, Deno, and browsers use the
same runtime model.

  </TabItem>
  <TabItem value="rust" label="Rust">

```bash
cargo add lumis
```

  </TabItem>
  <TabItem value="elixir" label="Elixir">

```elixir
def deps do
  [
    {:lumis, "~> 0.3"}
  ]
end
```

  </TabItem>
  <TabItem value="java" label="Java">

```xml
<dependency>
  <groupId>io.roastedroot</groupId>
  <artifactId>lumis4j</artifactId>
  <version>0.0.7</version>
</dependency>
```

  </TabItem>
  <TabItem value="cli" label="CLI">

```bash
npx @lumis-sh/cli --help
```

For a global install:

```bash
npm install -g @lumis-sh/cli
```

The installed binary is `lumis`. Rust users can also install from crates.io with `cargo install lumis-cli`.

  </TabItem>
  <TabItem value="browser" label="Browsers / CDN">

```html
<script type="module">
  import {highlight} from 'https://esm.sh/@lumis-sh/lumis';
  import {htmlInline} from 'https://esm.sh/@lumis-sh/lumis/formatters';
  import javascript from 'https://esm.sh/@lumis-sh/lumis/langs/javascript';
  import dracula from 'https://esm.sh/@lumis-sh/themes/dracula';

  const html = await highlight(
    'const x = 1',
    htmlInline({language: javascript, theme: dracula})
  );

  document.getElementById('output').innerHTML = html;
</script>
```

  </TabItem>
</Tabs>

## Notes

- JavaScript loads parser WASM files at runtime, persists verified bytes in Node or browser caches, and supports build-time caching. See [WASM and CDN](/advanced/wasm-and-cdn).
- Elixir loads missing root and injected parser WASM on demand; OTP releases can embed selected parsers with `mix lumis.parsers.cache`.
- For local JavaScript parser assets from npm, install per-language packages such as `@lumis-sh/wasm-rust` or bundle packages such as `@lumis-sh/wasm-bundle-web`.
- The CLI stores verified, content-addressed parsers and custom themes in a local data directory.
