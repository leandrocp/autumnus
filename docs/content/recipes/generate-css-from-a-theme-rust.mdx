---
sidebar_position: 13
slug: /recipes/generate-css-from-a-theme-rust
title: Generate CSS from a theme (Rust)
description: Generate linked CSS from a built-in Lumis theme in Rust.
---

# Generate CSS from a theme (Rust)

```rust
use lumis::themes;

let css = themes::get("catppuccin_latte").unwrap().css(true);
```
