---
sidebar_position: 6
slug: /recipes/terminal-output
title: Terminal output
description: Highlight code for terminal output with ANSI colors.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Terminal output

<Tabs groupId="runtime" queryString="lang" defaultValue="javascript">
  <TabItem value="javascript" label="JavaScript">

```js
import {highlight} from '@lumis-sh/lumis'
import {terminal} from '@lumis-sh/lumis/formatters'
import rust from '@lumis-sh/lumis/langs/rust'
import frappe from '@lumis-sh/themes/catppuccin_frappe'

const ansi = await highlight('fn main() {}', terminal({language: rust, theme: frappe}))
console.log(ansi)
```

  </TabItem>
  <TabItem value="rust" label="Rust">

```rust
use lumis::{highlight, TerminalBuilder, languages::Language, themes};

let ansi = highlight(
    "fn main() {}",
    TerminalBuilder::new()
        .language(Language::Rust)
        .theme(Some(themes::get("catppuccin_frappe").unwrap()))
        .build()
        .unwrap(),
);

println!("{}", ansi);
```

  </TabItem>
  <TabItem value="cli" label="CLI">

```bash
lumis highlight main.rs --theme catppuccin_frappe
```

  </TabItem>
</Tabs>
