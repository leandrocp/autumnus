Run:

```bash
pnpm install
pnpm start
```

This example imports `@lumis-sh/wasm-elixir` directly from `node_modules` and applies it with `withWasm()`.

This is the browser example. It uses Vite and runs in the local dev server.

Optional:

```bash
pnpm build
```

Open the local Vite URL.
