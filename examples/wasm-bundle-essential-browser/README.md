Run:

```bash
pnpm install
pnpm start
```

This browser example installs `@lumis-sh/wasm-bundle-essential`, imports `bundledWasms`, and combines it with `@lumis-sh/lumis/bundles/essential` via `withWasmBundle()`.

Optional:

```bash
pnpm build
```

Open the local Vite URL.
